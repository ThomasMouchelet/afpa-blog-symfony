{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h1>{{ post.title }}</h1>
		<span>{{ post.createdAt | date("d/m/Y H:i:s") }}</span>
		<p>
			{{ post.content }}
		</p>
	</div>
{% endblock %}
